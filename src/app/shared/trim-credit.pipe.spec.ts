import { TrimCreditPipe } from './trim-credit.pipe';

describe('TrimCreditPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimCreditPipe();
    expect(pipe).toBeTruthy();
  });
});
